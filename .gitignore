# build artifacts
boggart
boggart.exe
boggart-studio
boggart-studio.exe
build/
build-*/
*.o
*.a
*.lib
src/embedded.c

# clangd / editor index
.cache/

# vendored object files (legacy Makefile build)
src/vendor/lua/src/*.o
src/vendor/sqlite/sqlite3.o

# stray run artifacts
error.txt

# release packaging (.github/workflows/release.yml stages here, and running
# those steps by hand to debug them should not dirty the tree)
dist/
boggart-*.tar.gz
boggart-*.zip
sdl2.zip
